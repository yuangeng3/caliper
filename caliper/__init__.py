"""Caliper — honest, ancestry/age/sex-aware, fully-local facial geometry & skin self-analysis.

There is no single ideal face. Every metric is reported as a position within the
user's own (ancestry, sex, age) reference distribution, with a confidence band —
never as distance from a universal template, and never as one overall score.
Nothing leaves the device.
"""

__version__ = "0.1.0"
